{
    "hooks": {
        "SessionStart": [
            {
                "type": "command",
                "command": "node -e \"process.stdout.write(JSON.stringify({hookSpecificOutput:{hookEventName:'SessionStart',additionalContext:process.env.FAI_SESSION_CONTEXT}}))\"",
                "env": {
                    "FAI_SESSION_CONTEXT": "Azure Cost Optimizer participant workshop (Play 102). Start with README.md and ACO Workshop Builder. Sprint 1 is local; Sprint 2 deploys the supplied curated web experience, validates Agent ACO, generates a report, and reviews a screenshot. OpenAPI/MCP are bonus. Default to sample/cost-only and stop before Azure reads, writes, roles, model spend, public access, or deletion."
                },
                "timeout": 3
            }
        ]
    }
}